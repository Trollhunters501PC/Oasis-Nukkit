# Documentacion de executeCode del JSON
la opcion executeCode del json se usa para ejecutar codigo JS con las APIs de JSEngineNK
```json
{
    ...
    "executeCode": "console.info('Hola Mundo');",
    ...
}
```
es un String del codigo que ejecutara